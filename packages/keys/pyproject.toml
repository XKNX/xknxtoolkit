[project]
name = "xknx-keys"
version = "0.1.0"
description = "XKNX keys library"
requires-python = ">=3.12"
dependencies = [
    "xknx-models",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/xknx"]
